{
    "name": "kuikui",
    "version": "1.0.0",
    "description": "Anonymous real-time collaboration tool",
    "private": true,
    "workspaces": [
        "backend",
        "frontend"
    ],
    "scripts": {
        "dev": "npm run env:setup && concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
        "dev:backend": "npm run dev --workspace=backend",
        "dev:frontend": "npm run dev --workspace=frontend",
        "build": "npm run env:setup && npm run build --workspace=backend && npm run build --workspace=frontend",
        "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend",
        "test": "npm run test --workspace=backend && npm run test --workspace=frontend",
        "env:setup": "node scripts/env-setup.js",
        "env:validate": "node scripts/env-validate.js"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}